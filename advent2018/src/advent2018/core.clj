(ns advent2018.core
  (:require [advent2018.day9 :as current]
            [advent2018.utils :as utils])
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]

  (current/solve))

(-main)
